function Home() {
    return <div>
        <h1>Se você quer ficar rico, clica nesse botão</h1>
        <button onClick={() => alert("Parabéns todo seu saldo bancário sumiu!")}>Clique aqui!</button>
    </div>
}

export default Home;
